#include "fn_internal.h"

/* Compatibility translation unit retained intentionally.
 * The implementation has been split across smaller source files so
 * archive linking can pull in only the API pieces an application uses. */
